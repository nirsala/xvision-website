# xvision-website — הוראות לסוכנים

הריפו הזה משותף: ניר עובד בו ידנית וכמה רוטינות מתוזמנות דוחפות אליו.
הכללים כאן חלים על כל סוכן שעובד בריפו, בנוסף להוראות שבפרומפט של הרוטינה עצמה.

## דיווח במייל — חובה

**כל מייל שנשלח מרוטינה בריפו הזה חייב להיפתח בשם הרוטינה**, בשורה נפרדת בראש גוף
המייל, לפני כל תוכן אחר. ניר מקבל מיילים מכמה רוטינות שונות וצריך לזהות מיד מאיזו
מהן הגיע הדוח — גם כשהנושא דומה וגם כשקוראים מהטלפון.

הפורמט: שורה ראשונה בגוף המייל, בולטת ויזואלית, בנוסח

> **רוטינה: \<שם הרוטינה\>**

שמות הרוטינות המוכרות:

| שם הרוטינה | מה היא עושה | נושא המייל |
|---|---|---|
| סוכן SEO ותוכן יומי — xvision.co.il | שיפור יומי של 3–4 דפים לפי תור דינמי | `📈 xvision SEO יומי \| [תאריך]` |

רוטינה שאינה ברשימה — תוסיף את עצמה לטבלה בריצה הראשונה שבה היא נוגעת בקובץ הזה.

הכלל חל גם על מיילים חריגים (תקלה, "לא נמצאה עבודה", בקשת אישור), לא רק על דוח
ההצלחה הרגיל. גם ב-push notification כדאי שהמשפט הראשון יזהה את הרוטינה.

## הערות פתוחות בין ריצות — אימות לפני העברה

רוטינה שקוראת "הערה פתוחה" מריצה קודמת (ב-`seo/improved.log` או במייל קודם)
**לא מעבירה אותה הלאה כמות שהיא**. לפני שהיא מופיעה שוב בדוח, צריך:

1. **לבדוק אותה מחדש מול הקוד** — האם הממצא עדיין קיים? ריצה קודמת עלולה לדווח
   שניקוי "הושלם" בעוד הוא נגע רק בחלק מהמופעים (קרה ב-18.9 עם "500+ פרויקטים":
   28 מופעים שרדו בתוך `ItemList` JSON-LD).
2. **להצליב מול הכללים** — אם הפרומפט של הרוטינה כבר מכריע בנושא, ההערה נמחקת
   ולא עולה שוב. אל תבקש מניר הכרעה בדבר שכבר הוכרע.
3. **להפריד החלטה שלך מהחלטה שלו** — היגיינת תור, סינון דפים ותעדוף הם החלטות
   של הסוכן. רק טענה עובדתית על העסק (מספרים, לקוחות, דירוגים, התחייבויות) היא
   הכרעה של ניר.

הערה שלא עברה את שלושת הסעיפים — אל תכלול אותה בדוח.

### הכרעות שכבר נסגרו — אין להעלות שוב

- **דפי `noindex`** (ובכללם 15 מתוך 20 דפי "מומלץ-2026") — מחוץ לתור הרוטציה
  לצמיתות. שיפור SEO בדף שאינו מאונדקס לא מייצר קליקים. החלוקה מכוונת: ליסטיקלים
  דקים (~550 מילים) קיבלו `noindex` או אוחדו בהפניה לדף חזק יותר. החריגים היחידים
  הם `blog/מסכי-led-לקליניקות-ומרפאות-מומלץ-2026.html` ו-
  `blog/מסכי-led-לחנויות-אופנה-מומלץ-2026.html` — מאונדקסים, ולכן פריטי תור רגילים.
- **דפי אנגלית** — פריטי תור רגילים לכל דבר. קיים `/en/` וכל דפי האנגלית בבלוג
  מאונדקסים, כלומר האנגלית מכוונת. אין להציג "תרגום מול הוצאה מהתור" כדילמה.
- **באדג' "★ 4.9/5"** — הפרומפט מתיר במפורש טקסט דירוג גלוי בעמוד; רק הסימון
  בסכימה (`aggregateRating`) אסור, והוא כבר הוסר מכל הדפים. אין לדווח על זה שוב.

## תזכורות רוחביות

- אל תריץ `seo/content-gen.js` או `seo/daily.js`.
- `git pull --rebase` לפני עבודה; ב-commit רק נתיבים מפורשים — לעולם לא `git add -A`.
- אזורים שמשימות אחרות מתחזקות בלעדית: `llms.txt`, `robots.txt`, `sitemap.xml`
  (מלבד הרצת `seo/build-sitemap.js`), `blog/index.json` (מלבד `seo/build-blog-index.js`),
  `automation.html` ודף `lobby-cms`.
- מותג בתוכן: רק "Pixel by Keshet" ו-"Pixel CMS". לעולם לא "Xvision" (שם דומיין בלבד)
  ולא Xibo.
